<% if user_signed_in? %>
  <%= render 'user_navbar' %>
<% else %>
  <%= render 'visitor_navbar' %>
<% end %>

<<<<<<< HEAD
<div class="text-center mt-5">
  <h1><strong style="font: 1.2em Raleway, sans-serif";>Earth's Most Amazing Islands</strong></h1>
=======
<div class="text-center mt-4">
  <h1><strong style="font-family: 'Raleway', sans-serif;
    font-family: 'Roboto Mono', monospace;">Earth's Most Amazing Islands</strong></h1>
>>>>>>> master
</div>
 <hr class="styled-hr">
<div class="cards">
  <% @islands.each do |island| %>
    <div class="card-category">
      <%= link_to island_path(island), class: "text-decoration-none text-dark" do%>
        <%= render partial: 'card', locals: { island: island } %>
      <% end %>
    </div>
  <% end %>
</div>

<div class="mt-4" style="width: 100%; height: 600px;"
  data-controller="map"
  data-map-markers-value="<%= @markers.to_json %>"
  data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>">
</div>

<%= render partial: 'footer' %>
